# PngCut 更新说明

## 1.0.2 — 2026-09-18

本次发布更新 macOS 客户端的界面与动效，并增加手动填写输出文件夹路径的功能。

### 界面与动效

- 窗口标题统一为 PngCut，移除标题图标，缩小顶部标题栏。
- 首页提示简化为“拖入图片或文件夹”，删除批量处理辅助说明；图片图标移除蓝色背景并放大。
- 拖放虚线框居中，随窗口尺寸调整为内容区约 80% 宽、72% 高；中间文字与按钮保持原有尺寸。
- 设置页采用紧凑分组布局，缩小“无损 / 平衡”的间距，删除“本地有损压缩”说明；默认窗口可完整显示输出路径与 GIF 设置。
- 统一 PNG 序列提示弹窗的主、次按钮尺寸和风格，修复标题栏合并后弹窗点击区域不一致的问题。
- 更新任务列表的处理中、完成、失败与重试状态呈现，完善设置切换、模式切换与控件反馈动效；支持系统“减少动态效果”设置。

### 输出路径

- 选择“指定输出文件夹”后，可手动输入路径，也可通过“选择…”按钮打开文件夹选择器。
- 支持绝对路径、`~/`、中文及带空格的文件夹路径；回车或离开输入框时保存。
- 路径为空、相对路径、文件夹不存在、目标为文件或不可写入时显示就地提示，不替换先前保存的有效路径。

### 兼容性与安装

- 适用于 macOS 13 及以上，DMG 内含 Intel / Apple 芯片通用应用。
- 应用版本为 1.0.2，构建号为 3，沿用原有应用标识与设置。
- 安装前请退出旧版，再将 PngCut.app 拖入 Applications 替换。
- 安装包未进行 Developer ID 签名及 Apple 公证。

### 验证说明

- Release 构建通过，DMG 完整性、应用版本、通用架构及内置压缩工具检查通过。
- 单元与模型测试共 125 项：124 项通过、1 项可选集成测试跳过、0 项失败。

最终首页尺寸调整尚未完成自动化界面复核：最近一次界面测试因 macOS“启用 UI 自动化”认证超时而未能启动。这不表示界面测试已通过，也不包含 Instruments 帧率验证。
